# Public base URL of the deployed site (no trailing slash).
# Used for metadataBase, canonical URLs, hreflang alternates, sitemap and robots.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# --- Cloudflare Stream (private, signed-URL video playback) ---
# These are server-only secrets. Never expose them to the client and never commit
# real values. Locally put them in .env.local; in production use Cloudflare secrets
# (`wrangler secret put <NAME>`).

# Cloudflare account that owns the Stream videos.
CF_ACCOUNT_ID=""

# API token with Stream read access (used to list/inspect videos server-side).
CF_STREAM_API_TOKEN=""

# ID of the Stream signing key used to mint short-lived playback JWTs.
CF_STREAM_SIGNING_KEY_ID=""

# PEM (base64) private key paired with CF_STREAM_SIGNING_KEY_ID.
CF_STREAM_SIGNING_KEY_PEM=""
